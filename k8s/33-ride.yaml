apiVersion: v1
kind: Service
metadata:
  name: ride-service
  namespace: blablacar
spec:
  selector:
    app: ride
  ports:
    - port: 80
      targetPort: 8083
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ride
  namespace: blablacar
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ride
  template:
    metadata:
      labels:
        app: ride
    spec:
      containers:
        - name: ride
          image: blablacar-ride:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: blablacar-config
            - secretRef:
                name: blablacar-secret